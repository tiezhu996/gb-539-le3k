import {Component, inject} from '@angular/core'; import {NgFor} from '@angular/common'; import {AppStore} from '../stores/app.store'; import {scheduleApi} from '../api/schedules'; import {RuleEvidenceDrawerComponent} from '../components/common/RuleEvidenceDrawer';
@Component({standalone:true,imports:[NgFor,RuleEvidenceDrawerComponent],template:`<div class="page-head"><div><p class="eyebrow">04 / SCHEDULES</p><h1>曲线优化</h1><p class="description">把算法建议放在规则证据和窑炉边界旁，支持人工复核。读数变化会自动让旧计划过期。</p></div><button class="button primary" (click)="calculate()">运行一次仿真</button></div><div class="schedule-banner"><div class="banner-mark">∿</div><div><strong>算法版本 curve-v2.0</strong><p>同一输入哈希会复用历史结果，建议仅限安全边界内的小步调整。</p></div></div><p class="form-error">{{error}}</p><section class="workspace-panel"><div class="panel-head"><div><h2>计划版本</h2><p>过期计划只读并保留快照；重新计算的计划指向被替代的旧计划。</p></div><span class="count">{{store.schedules().length}} 个版本</span></div><div class="table-wrap"><table><thead><tr><th>计划</th><th>批次</th><th>风险</th><th>生命周期</th><th>过期原因 / 替代关系</th></tr></thead><tbody><tr *ngFor="let item of store.schedules()"><td><strong>{{item.id.slice(0,8)}}</strong><small>{{item.algorithm_version}} · v{{item.version}}</small></td><td>{{item.timber_lot_id.slice(0,8)}}</td><td><span class="risk low">{{item.defect_risk_score}} / 100</span></td><td><span class="state" [class.warm]="item.schedule_state!=='superseded'" [class.muted]="item.schedule_state==='superseded'">{{stateLabel(item.schedule_state)}}</span><small *ngIf="item.frozen_at">已冻结快照</small></td><td><ng-container *ngIf="item.schedule_state==='superseded'"><small>过期：{{reasonLabel(item.superseded_reason)}}</small><small *ngIf="item.superseded_by_id">被 <strong>{{item.superseded_by_id.slice(0,8)}}</strong> 替代</small><small *ngIf="!item.superseded_by_id">等待新计划替代</small></ng-container><ng-container *ngIf="item.schedule_state!=='superseded' && item.supersedes_schedule_id"><small>替代旧计划 <strong>{{item.supersedes_schedule_id.slice(0,8)}}</strong></small></ng-container><small *ngIf="item.schedule_state!=='superseded' && !item.supersedes_schedule_id">—</small></td></tr></tbody></table></div></section>`})
export class SchedulesPageComponent {
  readonly store=inject(AppStore);error='';
  stateLabel(state:string):string { return ({draft:'草稿',calculating:'计算中',proposed:'待审核',failed:'计算失败',reviewed:'已复核',accepted:'已接受',voided:'已作废',superseded:'已过期'} as Record<string,string>)[state] || state; }
  reasonLabel(reason:string):string { return ({reading_supplemented:'读数补录',reading_voided:'读数作废',reading_corrected:'读数修正'} as Record<string,string>)[reason] || reason || '—'; }
  async calculate(){const lot=this.store.lots()[0];if(!lot){this.error='请先登记批次';return}try{await scheduleApi.calculate(lot.id);await this.store.refresh()}catch(error){this.error=error instanceof Error?error.message:'计算失败'}}
}
